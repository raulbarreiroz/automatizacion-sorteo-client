import { Dialog } from "@mui/material";

const DialogCarga = () => {
  return (
    <Dialog
      style={{
        backgroundColor: "red",
      }}
      fullScreen
      open={true}
    ></Dialog>
  );
};

export default DialogCarga;
